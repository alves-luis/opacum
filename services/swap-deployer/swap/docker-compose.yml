version: "3.7"
services:
  app:
    build:
      context: .
      args:
        subdomain: miei-db
    container_name: swap
    expose:
      - "80"
    depends_on:
      - miei-db
    networks:
      - net-swap
  miei-db:
    image: postgres:12
    container_name: postgres
    expose:
      - "5432"
    environment:
      - POSTGRES_USER=swapper
      - POSTGRES_DB=swapper
      - POSTGRES_PASSWORD=secret
    networks:
      - net-swap

networks:
  net-swap:
    driver: bridge
